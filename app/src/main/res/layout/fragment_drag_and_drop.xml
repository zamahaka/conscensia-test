<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/dragParent"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/dragChild"
        android:layout_width="@dimen/dragChild_size"
        android:layout_height="@dimen/dragChild_size"
        android:contentDescription="@string/contentDescription_dragAndDropChild" />

</FrameLayout>